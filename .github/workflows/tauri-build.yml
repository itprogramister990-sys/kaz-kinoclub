name: Release Desktop App
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    env:
      NEXT_PUBLIC_API_URL: "https://kaz-kinoclub-backend.onrender.com"
      NEXT_PUBLIC_SUPABASE_URL: "https://ldzuxpwgnihvnfvqjsgb.supabase.co"
      NEXT_PUBLIC_SUPABASE_ANON_KEY: "sb_publishable_kPkp0yQToDTyxpqxdHzFug_Al5vVM7n"
      NEXT_PUBLIC_TURNSTILE_SITE_KEY: "0x4AAAAAAD4lSiu1zQOrF5eZ"
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, ubuntu-22.04, windows-latest]
    runs-on: ${{ matrix.platform }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm install

      - name: Build Desktop App
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: "./frontend"
          tagName: "desktop-app-${{ github.sha }}"
          releaseName: "KinoKlub Desktop App (v0.1)"
          releaseBody: "Automated desktop app build for Windows, macOS, and Linux."
          releaseDraft: true
          prerelease: false
